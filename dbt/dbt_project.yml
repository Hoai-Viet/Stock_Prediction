name: stock_analytics
version: 1.0
profile: stock_analytics

models:
  stock_analytics:
    staging:
      +materialized: table
    intermediate:
      +materialized: view
    marts:
      +materialized: table
      +schema: dwh
