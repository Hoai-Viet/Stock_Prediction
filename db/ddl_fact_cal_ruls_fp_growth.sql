CREATE TABLE dwh.fact_cal_rules_fp_growth (
    id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,

    antecedents TEXT,
    consequents TEXT,

    confidence DOUBLE PRECISION,
    lift DOUBLE PRECISION,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    X1  INTEGER DEFAULT 0,
    X2  INTEGER DEFAULT 0,
    X3  INTEGER DEFAULT 0,
    X4  INTEGER DEFAULT 0,
    X5  INTEGER DEFAULT 0,
    X6  INTEGER DEFAULT 0,
    X7  INTEGER DEFAULT 0,
    X8  INTEGER DEFAULT 0,
    X9  INTEGER DEFAULT 0,
    X10 INTEGER DEFAULT 0,
    X11 INTEGER DEFAULT 0,
    X12 INTEGER DEFAULT 0,
    X13 INTEGER DEFAULT 0,
    X14 INTEGER DEFAULT 0,
    X15 INTEGER DEFAULT 0,
    X16 INTEGER DEFAULT 0
);
